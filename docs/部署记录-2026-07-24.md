# 思源 Compass 部署记录（2026-07-24）

本文记录思源 Compass 在上海交通大学 AI+教育教学平台的部署状态，供后续维护、排障和版本交接使用。文档只记录技术事实，不包含密钥或个人账号信息。

## 1. 部署范围

- 对外地址：<https://ai4edu.sjtu.edu.cn/shengya/>
- 学生源代码：<https://github.com/labixiaoji/SIYUAN-COMPASS>
- 部署 fork：<https://github.com/zephyr221/SIYUAN-COMPASS>
- 部署分支：`sjtu-production`
- 部署基线：学生仓库 `main` 的 `c13b3df`
- 首次部署改造提交：`b203306`
- HTTPS 跳转修正提交：`8d44661`

本次没有修改问卷题目、题目解释规则、画像 Prompt、报告 Prompt 或管理员/学生原有业务流程。改造范围为：

1. 接入校内 DeepSeek。
2. 接入现有 ai4edu 门户的 jAccount 登录。
3. 支持 `/shengya/` 子路径部署。
4. 增加生产环境配置、Nginx 和 systemd 模板。
5. 关闭生产环境本地注册和密码登录入口。

## 2. 运行架构

```text
浏览器
  |-- /shengya/      -> 公网 Nginx -> 127.0.0.1:18121 静态前端
  |-- /shengya/api/  -> 公网 Nginx -> 127.0.0.1:18120 FastAPI
  |-- /auth/         -> 公网 Nginx -> /run/portal.sock 门户服务

FastAPI
  |-- PostgreSQL 16: 127.0.0.1:5432 / siyuan_compass
  |-- SJTU Models: https://models.sjtu.edu.cn/api/v1
```

服务资源：

| 项目 | 当前值 |
| --- | --- |
| systemd 服务 | `siyuan-compass.service` |
| 服务用户 | `siyuan-compass` |
| 后端监听 | `127.0.0.1:18120` |
| 静态站监听 | `127.0.0.1:18121` |
| 应用目录 | `/opt/siyuan-compass` |
| 应用配置 | `/etc/siyuan-compass.env` |
| 校内模型配置 | `/etc/course-ai.env` |
| 数据库 | `siyuan_compass` |
| 数据库角色 | `siyuan_compass` |

运维端当前使用 SSH 别名 `pj2-ext` 连接服务器。该别名是本机配置，不属于项目代码。

## 3. jAccount 接入

### 3.1 认证来源

思源 Compass 没有单独注册一套 jAccount OAuth 应用，而是复用同域门户 `portal` 已有的 OAuth 登录和 Flask 签名 session：

```text
/shengya/api/auth/jaccount/login
  -> /auth/jaccount/login
  -> jAccount OAuth
  -> /auth/jaccount/callback
  -> /shengya/api/auth/jaccount/callback
  -> 原请求页面
```

后端只验证门户签名 session 并读取 `jaccount` 和展示名，不保存 jAccount 密码或 OAuth access token，也不继承门户管理员身份。

### 3.2 管理员白名单来源

管理员身份不是根据教师或学生身份自动判断，而是由 ai4edu 根站点的 `portal` 项目维护 `ADMIN_JACCOUNTS` 白名单。

来源项目：

- 本机目录：`/Users/k/ai/course`
- SJTU GitLab：`git@git.ip.dev.sjtu.edu.cn:moran/course2026.git`
- GitHub 镜像：`git@github.com:zephyr221/course.git`
- 项目组件：`portal/`（上海交通大学 AI+教育教学资源管理平台）
- 源文件：`portal/deploy/portal.service`
- 服务器生效文件：`/etc/systemd/system/portal.service`

Git 历史来源：

- `15749ce9`（2026-05-31，`Add AI resource platform admin console`）首次加入 `moran,yzhu` 管理员名单。
- `eb5252ee`（2026-06-01，`Update admin access and console styling`）将名单扩展为当前值。

这两项变更都来自 `course2026` 的 portal 管理端开发历史，与思源 Compass 学生仓库无关。

门户在 OAuth 成功后仍会用 `username in ADMIN_JACCOUNTS` 生成 `is_admin`，供 portal 自己的管理端使用。思源 Compass 明确忽略该字段；jAccount 首次进入生涯助手时统一映射为 `student`。

生涯助手管理员由本项目数据库独立授权，不复用 portal 白名单。`admin` 是附加管理能力：管理员仍可填写问卷、查看自己的报告和反馈，同时可以进入管理后台。

管理员授权命令：

```bash
cd /opt/siyuan-compass/backend
.venv/bin/python scripts/manage_jaccount_admin.py add <jaccount> [<jaccount> ...]
.venv/bin/python scripts/manage_jaccount_admin.py remove <jaccount> [<jaccount> ...]
```

命令可以在账号首次登录前执行；首次登录会补充真实展示名，并保留已授予的角色。

## 4. DeepSeek 接入

生产环境配置：

```text
LLM_PROVIDER=deepseek
DEEPSEEK_BASE_URL=https://models.sjtu.edu.cn/api/v1
DEEPSEEK_MODEL=deepseek-chat
```

后端可以读取项目专用的 `DEEPSEEK_*` 变量，也可以复用服务器 `/etc/course-ai.env` 中的：

```text
AI_API_KEY
AI_API_BASE
AI_MODEL
```

密钥不在项目目录、Git 历史或本文档中。首次部署已通过最小请求验证实际响应模型为 `deepseek-chat`。

## 5. 服务器文件

| 文件 | 用途 |
| --- | --- |
| `/etc/systemd/system/siyuan-compass.service` | FastAPI 服务定义 |
| `/etc/siyuan-compass.env` | 数据库、认证和站点配置，权限 `0600` |
| `/etc/nginx/snippets/siyuan-compass-location.conf` | ai4edu 主站 `/shengya/` 路由 |
| `/etc/nginx/conf.d/siyuan-compass-static.conf` | 内部静态站 `18121` |
| `/etc/nginx/sites-enabled/ai4edu` | 引入思源 Compass location snippet |
| `/opt/siyuan-compass/backend/.venv` | 生产 Python 环境 |
| `/opt/siyuan-compass/frontend/dist` | `/shengya/` 前端构建产物 |

仓库中的对应模板在 `deploy/` 下。模板不含真实密码和密钥。

## 6. 前端构建

必须带子路径和生产认证开关构建：

```bash
cd frontend
VITE_BASE_PATH=/shengya/ \
VITE_API_BASE_URL=/shengya/api \
VITE_ENABLE_LOCAL_AUTH=false \
npm run build
```

构建后检查 `frontend/dist/index.html`，脚本和样式地址应以 `/shengya/assets/` 开头。

## 7. 更新流程

学生仓库继续作为产品开发上游；部署 fork 只维护校内认证、模型和服务器适配。

```bash
git fetch upstream
git switch sjtu-production
git merge upstream/main
```

合并后先在本地执行：

```bash
cd backend
.venv/bin/python -m pytest -q

cd ../frontend
npm run typecheck
VITE_BASE_PATH=/shengya/ \
VITE_API_BASE_URL=/shengya/api \
VITE_ENABLE_LOCAL_AUTH=false \
npm run build
```

发布代码时排除本地环境、依赖、数据库和离线镜像：

```bash
rsync -a \
  --exclude '.env' \
  --exclude '.git' \
  --exclude 'backend/.venv' \
  --exclude 'backend/.pytest_cache' \
  --exclude 'frontend/node_modules' \
  --exclude 'data/local-postgres' \
  --exclude '*.tar.gz' \
  --exclude '__pycache__' \
  ./ pj2-ext:/opt/siyuan-compass/
```

服务器更新后：

```bash
ssh pj2-ext '
  /opt/siyuan-compass/backend/.venv/bin/pip install -r /opt/siyuan-compass/backend/requirements.txt &&
  chown -R siyuan-compass:siyuan-compass /opt/siyuan-compass &&
  systemctl restart siyuan-compass
'
```

只有 Nginx 配置发生变化时才重新安装模板；必须先执行 `nginx -t`，成功后再 reload。

## 8. 验证清单

本次部署完成时已通过以下检查：

- 后端测试：`21 passed`。
- 前端 TypeScript 检查和生产构建通过。
- `siyuan-compass.service` 为 `active`。
- `/shengya/` 返回 200，静态资源返回 200。
- `/shengya` 正确跳转到 HTTPS `/shengya/`。
- `/shengya/api/llm/status` 返回 `deepseek`、`deepseek-chat` 和校内模型地址。
- 最小 DeepSeek 请求实际返回 `OK`。
- jAccount 登录入口正确跳转门户并携带 `/shengya/` 回跳地址。
- 正式登录页只显示 jAccount，不显示本地注册和密码登录。
- PostgreSQL 业务表已初始化。

常用检查：

```bash
ssh pj2-ext 'systemctl status siyuan-compass --no-pager'
ssh pj2-ext 'journalctl -u siyuan-compass -n 100 --no-pager'
ssh pj2-ext 'curl -fsS http://127.0.0.1:18120/health'
curl -fsS https://ai4edu.sjtu.edu.cn/shengya/api/llm/status
```

## 9. 回退原则

1. 发布前记录部署分支提交 SHA。
2. 代码问题优先切换到上一个已验证提交，再重新构建和发布。
3. 配置变更前保存单独备份；Nginx 备份不要放在 `sites-enabled` 中，避免被当作额外站点加载。
4. 不覆盖 `/etc/siyuan-compass.env`，除非本次任务明确是修改生产配置。
5. 数据库结构变更前先备份数据库。本次部署没有建立自动业务数据备份策略。

## 10. 当前边界

- 报告生成任务运行在 FastAPI 进程内；服务重启会中断当次模型 HTTP 请求，但任务输入、租约和进度保存在 PostgreSQL，租约到期后会自动恢复。
- 当前是单后端进程部署，默认最多同时运行 4 条生成链路，适合课程范围内的中小规模使用；扩大范围前仍需压测模型网关。
- `course2026/portal` 的管理员名单只作用于 portal；思源 Compass 不读取或复制该名单。
- 若未来迁移到独立云服务器，需要重新设计 jAccount 回调或可信身份转发，不能直接复制门户 session 密钥到不受控服务器。
- 项目后续业务功能以学生上游仓库为准，部署分支应尽量减少与业务代码的长期差异。

## 11. 2026-08-19 更新发布

学生上游自基线 `c13b3df` 起推进了 10 个提交，本次全部合入并发布。

| 项 | 值 |
| --- | --- |
| 上游合入点 | `student/main` 的 `ad33e30` |
| 合并提交 | `f8bea3e` |
| 发布提交 | `c02b9c6` |
| 备份时间戳 | `20260819-111819` |
| 备份位置 | `/var/backups/siyuan-compass/{code,db,env}-20260819-111819.*` |

### 上游带来的变化

样式与移动端适配、云端草稿（新表 `assessment_drafts`）、生成与语音的每日配额、
隐私页与数据自助清除、语音转写接口（讯飞录音文件转写）。
建表走启动时的 `CREATE TABLE IF NOT EXISTS`，本次重启已自动完成，无需手工迁移。

### 本次部署分支额外做的三件事

**一、语音默认关闭，且按钮不再显示。**
`SPEECH_PROVIDER` 默认 `disabled`，`/etc/siyuan-compass.env` 里一个 `SPEECH_*` 都没配，
所以线上不会有任何学生音频离开校园网。但上游把 `VoiceInputButton` 无条件渲染了出来，
关闭状态下学生能看见麦克风、能录满 60 秒，最后只拿到 503。
本分支加了一道门禁：挂载时查 `/api/speech/status`，`configured` 为假就整个控件不渲染。
判断逻辑单独放在 `frontend/src/api/speechAvailability.ts`，不改上游的 `api/speech.ts`，
下次 merge 不会冲突。问卷里多个字段各挂一个按钮实例，Promise 缓存在模块级，整页只发一次请求。

将来若要启用，配齐 `SPEECH_XFYUN_APP_ID` / `SPEECH_XFYUN_API_KEY` / `SPEECH_XFYUN_API_SECRET`
并把 `SPEECH_PROVIDER` 改成 `xfyun_file`，按钮会自动出现，前端无需改动。
启用前需要先确认学生录音出校是否符合数据合规要求。

**二、清掉 `frontend/vite.config.js`。**
上游仓库里提交了 `vite.config.ts` 的编译产物 `vite.config.js` 和 `vite.config.d.ts`。
vite 解析配置时 `.js` 优先于 `.ts`，这两个文件会顶掉 `vite.config.ts`——
而 `base: /shengya/` 的逻辑只写在 `.ts` 里。真按上游的样子构建，产物会引用 `/assets/` 而不是
`/shengya/assets/`，线上是一个不报任何错的白页。本次已删除并写进 `.gitignore`。

**三、`python-multipart` 提为硬依赖。**
语音路由用了 `File`/`Form`，缺这个包 FastAPI 在注册路由时就抛 `RuntimeError`，
和语音开没开无关——服务会直接起不来。

### 验证结果

```text
后端 pytest                 73 passed（上次基线 21）
前端 vitest                 24 passed
前端 typecheck              通过
构建产物                     dist/index.html 引用 /shengya/assets/，无裸 /assets/
打包产物「注册」命中数        0（本地注册入口已 tree-shake 掉）
/health                     200 {"status":"ok"}
/shengya/                   200
/shengya                    301 -> https://ai4edu.sjtu.edu.cn/shengya/（未降级 http）
/shengya/assets/*.js|css    200
/shengya/api/llm/status     provider=deepseek configured=true
jAccount 登录入口            302 -> 门户 -> jaccount.sjtu.edu.cn/oauth2/authorize
SPA 直达路由                 /shengya/assessment、/shengya/privacy 均 200
邻居应用                     /notes/、/agent/、/thermo/ 均 200
数据库                       assessment_drafts 已自动建出，共 11 张表
```

### 发布步骤（与第 7 节相同，本次实际执行）

1. 备份代码、数据库、`/etc/siyuan-compass.env` 到 `/var/backups/siyuan-compass/`。
2. 本地按第 6 节参数构建前端，断言产物路径。
3. rsync **不带** `--delete`：服务器上 `docs/` 有几份不在仓里的问卷原始 docx，
   带 `--delete` 会连它们一起删掉。旧的 hash 产物单独用一条限定在
   `frontend/dist/assets/` 的 `rsync --delete` 清理。
4. `.venv/bin/pip install -r requirements.txt`（本次新增 `python-multipart`）。
5. `chown -R siyuan-compass:siyuan-compass /opt/siyuan-compass` —— rsync 会把属主写成本地 uid，
   这一步不能省。
6. `systemctl restart siyuan-compass`，然后走上面那份验证清单。

### 待反馈给学生的两个上游问题

1. `fetchSpeechStatus` 已实现但没有任何调用方，语音按钮无条件渲染，
   后端未配置时用户只会拿到 503。
2. `vite.config.js` / `vite.config.d.ts` 是编译产物，不应提交，会静默顶掉 `vite.config.ts`。

## 12. 2026-08-28 高峰故障处理与任务护航发布

### 故障判断

高峰期间数据库共记录 213 个任务，其中 165 个停在画像阶段、28 个停在报告阶段、
18 个停在任务准备阶段，只有 1 个完成。画像失败的中位耗时为 180.2 秒，和原
`LLM_TIMEOUT_SECONDS=180` 完全吻合；报告阶段失败集中在约 300—360 秒。故障主因是大量任务
无全站并发上限地同时进入校内模型，非流式长文本响应又超过了单次 180 秒读取超时，不是本机
CPU 或内存耗尽。

准备阶段的 18 个失败来自另一处脱敏边界错误：极短的姓名、学号或联系方式可能作为子串误替换
内部 `userId`。该问题已通过保留系统关联字段的方式修复，并增加回归测试。

旧版本会在任务刚创建时删除云端草稿，并在失败时清除任务临时输入，因此这些历史终态任务不能
在服务器端直接自动重放。发布前数据库仍有 217 份云端草稿；没有云端草稿的学生需要使用原浏览器
保留的 7 天本地草稿重新提交。新版本不再在任务刚创建时删除云端草稿，失败任务的恢复输入保留到
终态任务清理期限。

### 发布内容

| 项 | 值 |
| --- | --- |
| 发布提交 | `434c654` |
| 备份时间戳 | `20260828-152303` |
| 备份位置 | `/var/backups/siyuan-compass/{code,db,env}-20260828-152303.*` |
| 模型读取超时 | `LLM_TIMEOUT_SECONDS=600` |
| 同时生成上限 | `GENERATION_MAX_CONCURRENCY=4` |
| 发布后维护状态 | `ASSESSMENT_SUBMISSION_MAINTENANCE=false` |

本次增加了维护开关、并发闸门、排队提示、失败输入保留、任务最小化日志和更低频的前端轮询。
模型读取允许 600 秒，但连接建立仍限制为 10 秒，请求写入和连接池等待限制为 30 秒，避免把网络
不可达误当成长文本生成。

### 验证结果

```text
后端 pytest                         82 passed
前端 vitest                         27 passed
前端 typecheck                      通过
/shengya/                           200
/shengya/assets/index-C4QaxrZw.js   200
/health                             200 {"status":"ok"}
/shengya/api/llm/status             deepseek configured=true
/shengya/api/assessment-maintenance active=false
最小 DeepSeek 请求                  7.01 秒返回，content_present=true
发布后即时错误日志                  无新增异常
```
